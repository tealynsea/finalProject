extends layout

block content 
  .container
    nav.navbar.navbar-inverse.navbar-fixed-top(role="navigation")
        .container    

            .navbar-header
                button(type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse")
                    span.sr-only Toggle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href='/')
                    p WCMN

            .collapse.navbar-collapse.navbar-exl-collapse
                ul.nav.navbar-nav.navbar-right
                    li 
                        a(href="/resources") Resources
                    li.dropdown
                        a(href='mailto:tealynsea@hotmail.com?Subject=Feedback WCMN') Feedback
                     
                          
    //- Error comes through via the context passed when calling res.render('login', {...})
    //- You'll see this in controllers/authentication.js
    //- If the error exists, we'll display it.
    
    if error
      h3= error

      //- Form for handling existing user logins.
    h1#signinForm Existing Users
      form.form-horizontal(action="/auth/login", method="post", role="form")
        .form-group
          label.col-sm-2.control-label(for='username') Username
          .col-sm-10    
            input#username.form-control(type="text", name="username", placeholder="Username")
        .form-group
          label.col-sm-2.control-label(for='password') Password
          .col-sm-10
            input#password.form-control(type="password", name="password", placeholder="Password")
        .form-group
          .col-sm-2.col-sm-10
            button.btn.btn-default(type='submit') Sign in
            //- input(type="submit")

  //- Form for allowing new users to register.
  //- h1 Register
  //- form(action="/auth/signup", method="post")
  //-   input(type="text", name="username", placeholder="Username")
  //-   input(type="email", name="email", placeholder="Email")
  //-   input(type="password", name="password", placeholder="Password")
  //-   input(type="submit")